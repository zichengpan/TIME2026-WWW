# GitHub Pages 部署設定指南

## 📋 前置條件

已完成以下檔案的建立：
- ✅ `.github/workflows/deploy.yml` - GitHub Actions 工作流程
- ✅ `_config.yml` - Jekyll 配置檔案
- ✅ `Gemfile` - Ruby 依賴項
- ✅ `.gitignore` - 排除檔案設定

## 🚀 部署步驟

### 1. 推送程式碼到 GitHub

```bash
# 添加所有新檔案
git add .github/ _config.yml Gemfile README.md .gitignore

# 提交變更
git commit -m "Add GitHub Pages configuration and deployment workflow"

# 推送到遠端
git push origin QuickFix
```

### 2. 在 GitHub 上啟用 GitHub Pages

1. 前往您的 GitHub 倉庫頁面：
   `https://github.com/zichengpan/TIME2026-WWW`

2. 點擊 **Settings** (設定)

3. 在左側選單中，點擊 **Pages**

4. 在 "Build and deployment" 區域：
   - **Source**: 選擇 `GitHub Actions`
   - 這會啟用自動化部署

5. 儲存設定

### 3. 觸發首次部署

推送程式碼後，GitHub Actions 會自動執行：

1. 前往倉庫的 **Actions** 標籤頁
2. 您會看到 "Deploy to GitHub Pages" 工作流程正在執行
3. 等待工作流程完成（通常需要 1-2 分鐘）

### 4. 訪問您的網站

部署成功後，您的網站將在以下網址可用：

```
https://zichengpan.github.io/TIME2026-WWW/
```

## 🔍 檢查部署狀態

### 查看 GitHub Actions 日誌

1. 前往倉庫的 **Actions** 標籤頁
2. 點擊最新的工作流程執行
3. 查看 "build" 和 "deploy" 步驟的詳細日誌

### 常見問題排查

**問題：404 頁面未找到**
- 確認 GitHub Pages 設定中 Source 為 "GitHub Actions"
- 檢查 `_config.yml` 中的 `baseurl` 設定是否正確
- 等待幾分鐘讓 DNS 傳播

**問題：樣式未載入**
- 檢查 `_config.yml` 中的 `baseurl: "/TIME2026-WWW"`
- 確認所有 CSS/JS 路徑使用相對路徑

**問題：工作流程執行失敗**
- 前往 Actions 查看錯誤訊息
- 檢查 YAML 語法是否正確
- 確認所有必要檔案都已提交

## 📝 後續更新

每次推送到 `master`、`main` 或 `QuickFix` 分支時，網站都會自動重新部署。

```bash
# 進行修改後
git add .
git commit -m "Update website content"
git push origin QuickFix
```

## 🎯 測試清單

部署完成後，請測試以下功能：

- [ ] 主頁正常顯示
- [ ] 導航選單功能正常
- [ ] 所有內頁可正常訪問
  - [ ] `/pages/calls.html`
  - [ ] `/pages/important-dates.html`
  - [ ] `/pages/media.html`
- [ ] CSS 樣式正確載入
- [ ] JavaScript 功能正常（漢堡選單等）
- [ ] 圖片正確顯示
- [ ] 外部連結正常運作

## 💡 提示

1. **本地測試**: 使用 `bundle exec jekyll serve` 在本地測試
2. **快速預覽**: 推送到 QuickFix 分支進行測試，確認無誤後合併到 master
3. **監控**: 訂閱 GitHub Actions 通知以接收部署狀態

## 📧 需要幫助？

如有問題，請檢查：
- GitHub Actions 執行日誌
- Jekyll 建置錯誤訊息
- 瀏覽器開發者控制台的錯誤
